from setuptools import find_packages, setup

setup(
    name='eduschedule_lib',
    packages=find_packages(),
    version='1.0.0',
    description='My eduschedule_lib app lib',
    author='Maksim Zhak',
    install_requires=[],
)